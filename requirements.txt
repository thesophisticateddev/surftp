# SURFTP runtime dependencies only. Dev/build tooling lives in
# requirements-dev.txt. This file is what ships in the frozen bundle, so
# everything here is imported by the app (see surftp/ for the import scan).
aioftp==0.28.0
argon2-cffi==25.1.0
asyncssh==2.24.0
bcrypt==5.0.0
cryptography==50.0.1
duckdb==1.5.5
msgpack==1.2.2
platformdirs==4.11.5
pyte==0.8.2
rich==15.0.0
textual==8.2.8