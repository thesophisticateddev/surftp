<?xml version="1.0" encoding="UTF-8"?>
<!--
  Hardened-runtime entitlements for the notarised macOS build.

  Notarisation requires the hardened runtime, and the hardened runtime breaks
  a CPython bundle by default: the interpreter allocates writable-executable
  memory for ctypes/cffi trampolines, and the bundled .so files are signed by
  us rather than by Apple. argon2-cffi, bcrypt, cryptography and duckdb all
  come in through cffi or a Rust/C extension, so without these three the
  binary is signed, notarised, and then crashes on first launch.

  Nothing here relaxes sandboxing, network or file access — SURFTP is not
  sandboxed, and these are the narrowest set that lets a frozen CPython run.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
